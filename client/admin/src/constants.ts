// Authentication
export const TOKEN_EXPIRY_HOURS = 16;
export const TOKEN_EXPIRY_MS = TOKEN_EXPIRY_HOURS * 60 * 60 * 1000;

// Polling intervals
export const TABLE_POLLING_INTERVAL_MS = 5 * 60 * 1000; // 5 minutes
export const MENU_POLLING_INTERVAL_MS = 5 * 60 * 1000; // 5 minutes

// Mock API delays
export const MOCK_API_DELAY_MS = 500;
